name: Pull Request CI

on:
  pull_request:
    branches: [ "main" ]

jobs:
  pull-request:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Run Lint
      run: make lint

    - name: Run Test
      run: make test

